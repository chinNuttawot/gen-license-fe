# Frontend Port
PORT=3000

# Backend API Base (Direct connection for binary downloads)
NEXT_PUBLIC_API_BASE=http://localhost:4000

# API Proxy Path (Used for generation calls via Next.js rewrites)
NEXT_PUBLIC_PROXY_BASE=/api
